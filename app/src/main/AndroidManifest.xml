<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ma.makeshipei_screen">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".percent_method1.percent_method_one.MainActivity">

        </activity>
        <activity android:name=".percent_method1.percent_method_one.PercentRelativeLayout_Activity"></activity>
        <activity android:name=".percent_method1.percent_method_one.ThreeActivity">

        </activity>


       <!-- 第二种百分比 布局-->

        <activity android:name=".percent_method1.percent_method_two.ItemActivity">

        </activity>

        <activity android:name=".percent_method1.percent_method_two.ListViewTestActivity"></activity>

        <activity android:name=".percent_method1.percent_method_two.MainActivity_two">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
    <uses-permission android:name="android.permission.INTERNET" /> <!-- 联网 -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" /> <!-- 读取IMEI，IMSI,基站等数据 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" /> <!-- 判断网络状态 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" /> <!-- 往SDCard写入数据权限 -->
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" /> <!-- 在SDCard中创建与删除文件权限 -->
</manifest>
